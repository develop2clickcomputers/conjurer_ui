import { Routes } from '@angular/router';

import { XmlfileComponent } from './xmlfile.component';

export const XMLRoutes: Routes = [
    {
        path: 'xmlfile', component: XmlfileComponent
    }
]
